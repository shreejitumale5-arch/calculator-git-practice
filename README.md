# Git & GitHub Calculator Practice Project

A tiny calculator made with HTML, CSS, and JavaScript.

## Purpose

This project is intentionally simple. Use it to practice:

* `git init`
* `git status`
* `git add`
* `git commit`
* `git log`
* `git diff`
* Editing files
* `.gitignore`
* `git restore`
* `git rm`
* Branches
* Merge
* Merge conflicts
* GitHub push/pull/clone
* Tags
* GitHub Pages hosting

## Suggested Progression

1. Create the first commit.
2. Edit the calculator and make another commit.
3. Practice `git diff`.
4. Add a local-only file and ignore it.
5. Create a feature branch.
6. Merge the feature branch.
7. Create a deliberate merge conflict and resolve it.
8. Push to GitHub.
9. Clone the repository into another folder.
10. Enable GitHub Pages.

## Project Structure

```text
calculator-project/
├── index.html
├── style.css
├── script.js
├── README.md
├── .gitignore
└── .env.example
```

## Live Demo

This project is hosted using GitHub Pages.
